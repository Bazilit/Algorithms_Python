s = input()
t = input()
for i in range(len(t)):
	if t[i] in s:
		s = s.replace(t[i], '', 1) #Если t[i] элемент в массиве s, идет замена данного элемента на пустое значение. Параметр 1, это замена текущего занчения. Если убрать заменит все занчения в массиве при соответствие.
	else:
		print(t[i])
